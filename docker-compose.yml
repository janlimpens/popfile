services:
  popfile:
    build: .
    ports:
      - "7070:7070"
    environment:
      - POPFILE_LOCAL=${POPFILE_LOCAL:-0}
      - POPFILE_PASSWORD=${POPFILE_PASSWORD:-}
    volumes:
      - popfile-data:/data

volumes:
  popfile-data:
